services:
  api1:
    build: .
    container_name: api1
    hostname: api1
    environment:
      - SOCKET_PATH=/tmp/api1.sock
      - FOREIGN_STATE=api2
      - MODE=🔥
    volumes:
      - rb2025:/tmp/
    networks:
      - payment-processor
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.6"
          memory: 155MB

  api2:
    build: .
    container_name: api2
    hostname: api2
    environment:
      - SOCKET_PATH=/tmp/api2.sock
      - FOREIGN_STATE=api1
      - MODE=🔥
    volumes:
      - rb2025:/tmp/
    networks:
      - payment-processor
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.6"
          memory: 155MB
  map8x32:
    image: map8x32
    container_name: map8x32
    volumes:
      - rb2025:/tmp/
    networks:
      - payment-processor
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: 40MB

  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - rb2025:/tmp/
    depends_on:
      - api1
      - api2
    networks:
      - payment-processor
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: 40MB

volumes:
  rb2025:
    driver: local

networks:
  payment-processor:
    external: true
    driver: bridge
